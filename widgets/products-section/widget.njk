<section class="py-16 bg-gray-50">
  <div class="container mx-auto">
    {% if widget.data.products | length > 0 %}
    <div class="flex flex-col gap-8">
      <!-- Section Header -->
      <div class="text-center mb-8">
        <h2 class="text-4xl font-bold text-gray-900 mb-4 font-cairo">منتجاتنا المميزة</h2>
        <p class="text-xl text-gray-600 font-cairo">اكتشف تشكيلة واسعة من المنتجات عالية الجودة</p>
      </div>
      
      <!-- Products Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-6">
        {% for product in widget.data.products %}
        {%ui "product-card.njk" ,product=product ,showModal=true%}
        {% endfor %}
      </div>
      
      <!-- View More Button -->
      <div class="text-center mt-8">
        <a href="/collections" class="inline-flex items-center gap-3 bg-[#003033] hover:bg-[#004d4d] text-white font-bold py-4 px-8 rounded-lg shadow-sm hover:shadow-md transition-all duration-300 font-cairo">
          <span>عرض المزيد من المنتجات</span>
          <i class="fi fi-rr-angle-double-small-left text-xl"></i>
        </a>
      </div>
    </div>
    {% else %}
    {%ui "empty.njk" %}
    {% endif %}
  </div>
</section>

